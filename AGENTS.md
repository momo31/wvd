# Repository Automation Rules

## 실행 파일 빌드 시 필수 참조

실행 파일을 빌드하거나 `dist\\wvd`에 배포할 때는 먼저 [`docs/build-and-release.md`](docs/build-and-release.md)를 확인한다. 테스트는 개발 단계에서 완료된 것으로 간주한다.

- 빌드는 별도 `build\\release-stage-*` 경로에서 수행하고 종료한다. 사용자가 배포를 명시적으로 요청하지 않으면 실행 중인 배포본과 `dist\\wvd`를 건드리지 않는다.
- 빌드 단계에서는 테스트 스위트, exe 실행, 스모크 테스트, Tcl/Tk·리소스·해시 검증을 수행하지 않는다.
- 배포할 때는 `_internal`, `wvd.exe`, `CHANGES_LOG.md`만 교체한다. 유일한 검증은 배포 전후 `config.json`, `logs`, `audit` 보존 확인이다.

## Git 작업 제한

- Codex는 Git 커밋, 원격 push, PR 생성·갱신을 수행하지 않는다. 변경과 검증 결과만 사용자에게 보고하며 Git 반영은 사용자가 직접 수행한다.
- PR의 base는 사용자 저장소의 기본 통합 브랜치(현재 `master`)로 한다.
- PR 제목과 본문은 실제 커밋 및 diff를 기준으로 작성하며 다음 내용을 포함한다.
  - 구현하거나 수정한 기능
  - 주요 동작 변화와 위험 요소
  - 실행한 테스트와 결과
  - 실행하지 못한 검증 또는 남은 제한 사항
- `upstream`은 패치 확인 및 fetch 전용 원격이다. `upstream`에는 절대 푸시하거나 PR을 생성하지 않는다.
- GitHub 인증 정보는 Git Credential Manager 같은 로컬 자격 증명 저장소에서만 사용한다. 토큰을 원격 URL, 저장소 파일, 커밋, PR 본문 또는 로그에 기록하지 않는다.
- 런타임 로그, 개인 설정, 분석 초안 등 개발 변경과 무관한 파일은 자동 커밋하거나 PR에 포함하지 않는다.
- 인증·네트워크·권한 문제로 PR 생성 또는 갱신에 실패하면 성공했다고 간주하지 않는다. 로컬 커밋과 브랜치를 보존하고 정확한 실패 지점을 사용자에게 알린다.

## 세션 인계

- 사용자가 `이전 작업을 이어서 해줘` 또는 같은 의미로 요청하면 저장소 루트의 `SESSION_HANDOFF.md`가 있는지 먼저 확인하고, 있으면 완전히 읽은 뒤 현재 파일·프로세스·로그 상태를 재검증하여 작업을 이어간다.
- 인계 내용은 오래된 외부 상태일 수 있으므로 그대로 단정하지 않는다. 완료된 항목은 반복하지 않고 미완료 항목부터 진행하며, 작업 상태가 달라지면 `SESSION_HANDOFF.md`를 갱신한다.

## 업스트림 패치 로케일 검증

- `upstream`에서 패치를 가져오거나 병합한 경우, 새로 추가되거나 변경된 UI 문자열과 데이터 기반 표시 항목에 지원 언어별 로케일 처리가 적용되어 있는지 반드시 확인한다.
- 한국어 UI에서 중국어 원문이 폴백으로 노출되지 않도록 퀘스트명·카테고리 같은 목록 데이터의 번역 키 또는 한국어 매핑을 점검한다.
- gettext를 사용하는 문자열은 `.po` 번역 누락과 `.mo` 컴파일 결과의 불일치를 확인하고, 하드코딩된 표시 문자열도 함께 점검한다.
- 로케일 누락이 다시 유입되지 않도록 가능한 경우 자동화 테스트를 추가하거나 기존 로케일 검증 테스트를 갱신하고 실행 결과를 PR 본문에 기록한다.

## 프로그램 핵심 런타임 규칙

### 상자 선택창과 결과창은 절대 혼동하지 않는다

상자 화면에서 `dialogueNext`를 결과창으로 오인하는 문제는 최우선 방지 대상이다. 다음 불변 규칙을 모든 상자·결과창 인식 로직에 적용한다.

- `chestFlag`, `whowillopenit`, `chestOpening` 중 하나라도 검출되면 현재 화면은 상자 상호작용 화면으로 확정한다. 이 프레임에서는 `dialogueNext` 스킵 탭을 수행하지 않는다.
- 상자 표식이 사라진 뒤 결과창 전용 표식이 있으면 함께 검증한다. `dialogueNext`만 사용할 수 있는 경로는 상자 표식 전부 부재, 높은 명시적 임계값, bounded retry를 모두 만족해야 한다.
- 낮은 신뢰도의 단일 프레임 매칭, 특히 움직이는 배경·장식 영역의 매칭은 오탐으로 간주한다. 화면 전환 또는 연속 프레임 확인 없이 동일 좌표를 반복 탭하지 않는다.
- 감지된 좌표를 탭한 뒤 화면 상태가 변하지 않으면 즉시 상자 상태 판정으로 되돌아가야 한다. 동일 조건의 무제한 반복은 금지하고, bounded retry와 상태 재식별을 사용한다.
- `StateChest`, `CheckIf`, `dialogueNext` 템플릿 또는 관련 ROI/임계값을 변경할 때는 상자 선택창 fixture에서 `Open` 처리가 우선되고 결과창 스킵 좌표가 탭되지 않는 회귀 테스트를 함께 추가·실행한다.

이 규칙은 현재 확인된 오탐 사례(상자 선택창에서 `dialogueNext`가 하단 배경에 매칭되어 버튼 밖을 반복 탭함)를 재발 방지하기 위한 최우선 런타임 불변 조건이다.
